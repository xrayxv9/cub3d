#include "cub3D_bonus.h"

// int	check_portal_textures(t_data *data)
// {
	// int	fd;
// 
	// fd = open("bonus/textures/portal_launcher.png", O_RDONLY);
	// if (fd == -1)
		// return (0);
	// close(fd);
	// fd = open("bonus/textures/crosshair.png", O_RDONLY);
	// if (fd == -1)
		// return (0);
	// close(fd);
	// fd = open("bonus/textures/portals/blue.png", O_RDONLY);
	// if (fd == -1)
		// return (0);
	// close(fd);
	// fd = open("bonus/textures/portals/orange.png", O_RDONLY);
	// if (fd == -1)
		// return (0);
	// close(fd);
	// data->portal_images[CROSSHAIR] = mlx_new_image_from_file(data->game,
			// "bonus/textures/crosshair.png", NULL, NULL);
	// data->portal_images[BLUE_IMAGE] = mlx_new_image_from_file(data->game,
			// "bonus/textures/portals/blue.png", NULL, NULL);
	// data->portal_images[ORANGE_IMAGE] = mlx_new_image_from_file(data->game,
			// "bonus/textures/portals/orange.png", NULL, NULL);
	// return (1);
// }
